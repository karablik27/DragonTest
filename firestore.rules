rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Любой документ в любой коллекции
    match /{document=**} {
      // Доступ разрешён только если пользователь авторизован
      allow read, write: if request.auth != null;
    }
  }
}